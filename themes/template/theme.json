{
  "_comment": "Clawd Theme Template — copy this folder to {userData}/themes/your-theme-id/ and customize",
  "_scaffoldOnly": true,

  "schemaVersion": 1,
  "name": "My Theme",
  "author": "Your Name",
  "version": "1.0.0",
  "description": "A short description of your theme",
  "_comment_license": "Optional: if you add a license field, treat it as descriptive metadata only. It should describe assets you own and does not grant rights by itself.",

  "viewBox": {
    "_comment": "Logical canvas in SVG units. All assets should share this aspect ratio.",
    "x": -15,
    "y": -25,
    "width": 45,
    "height": 45
  },

  "layout": {
    "_comment": "Optional but recommended when your visible character height differs a lot from the raw viewBox. contentBox is the character's visible body area in viewBox units.",
    "contentBox": { "x": -4, "y": -3, "width": 23, "height": 20 },
    "centerX": 7.5,
    "baselineY": 17,
    "visibleHeightRatio": 0.58,
    "baselineBottomRatio": 0.05
  },

  "eyeTracking": {
    "_comment": "Set enabled:true only for states that really need cursor tracking. Only eyeTracking.states entries must be SVG.",
    "enabled": true,
    "states": ["idle"],
    "eyeRatioX": 0.5,
    "eyeRatioY": 0.5,
    "maxOffset": 3,
    "bodyScale": 0.33,
    "shadowStretch": 0.15,
    "shadowShift": 0.3,
    "ids": {
      "eyes": "eyes-js",
      "body": "body-js",
      "shadow": "shadow-js"
    },
    "shadowOrigin": "7.5px 15px"
  },

  "states": {
    "_comment": "Each state can be an array of filenames or an object like { files: [...], fallbackTo: \"attention\" }. SVG, GIF, APNG, WebP, PNG, JPG, JPEG all work. Only eyeTracking.states entries MUST be SVG.",
    "idle":         ["idle-follow.svg"],
    "thinking":     ["thinking.gif"],
    "working":      ["typing.gif"],
    "juggling":     ["juggling.gif"],
    "error":        { "fallbackTo": "attention" },
    "attention":    ["happy.gif"],
    "notification": ["notification.gif"],
    "sleeping":     ["sleeping.gif"],
    "waking":       ["waking.gif"]
  },

  "_comment_optional_states": "Optional extras. fallbackTo is only allowed on error / attention / notification / sweeping / carrying / sleeping. Omit other optional states only if you are okay with them visually degrading to idle.",

  "sleepSequence": {
    "_comment": "full = yawning/dozing/collapsing/waking all need real files. direct = go straight to sleeping after mouseSleepTimeout; waking becomes optional.",
    "mode": "direct"
  },

  "workingTiers": [
    { "_comment": "Show different animations based on concurrent session count", "minSessions": 3, "file": "building.gif" },
    { "minSessions": 2, "file": "juggling.gif" },
    { "minSessions": 1, "file": "typing.gif" }
  ],

  "jugglingTiers": [
    { "_comment": "Optional subagent-specific overrides. Omit this whole block to use states.juggling[0] if you provide that state.", "minSessions": 2, "file": "conducting.gif" },
    { "minSessions": 1, "file": "juggling.gif" }
  ],

  "idleAnimations": [
    { "_comment": "Random animations played during idle. Omit this whole array (or leave it empty) to keep idle on states.idle[0]. duration in ms.", "file": "idle-look.gif", "duration": 6500 }
  ],

  "timings": {
    "_comment": "All values in milliseconds. Omit to use defaults.",
    "mouseIdleTimeout": 20000,
    "mouseSleepTimeout": 60000
  },

  "hitBoxes": {
    "_comment": "Clickable area in viewBox units. 'default' is required.",
    "default": { "x": -1, "y": 5, "w": 17, "h": 12 },
    "sleeping": { "x": -2, "y": 9, "w": 19, "h": 7 }
  },
  "sleepingHitboxFiles": ["sleeping.gif"],

  "reactions": {
    "_comment": "Click/drag reaction animations. Omit the whole block to disable click and drag reactions.",
    "drag":       { "file": "react-drag.gif" },
    "clickLeft":  { "file": "react-left.gif",  "duration": 2500 },
    "clickRight": { "file": "react-right.gif", "duration": 2500 },
    "double":     { "files": ["react-double.gif"], "duration": 3500 }
  },

  "sounds": {
    "_comment": "Map logical sound names to audio files in sounds/ subdirectory. Omit this block to use built-in defaults. Set a value to null to disable that sound.",
    "complete": "complete.mp3",
    "confirm":  "confirm.mp3"
  },

  "miniMode": {
    "_comment": "Set supported:false (or remove this block entirely) to disable mini mode. If supported=true, validator expects all 8 mini states.",
    "supported": false
  },

  "objectScale": {
    "_comment": "Fine-tune the rendered size relative to viewBox. Defaults work for most themes.",
    "widthRatio": 1.9,
    "heightRatio": 1.3,
    "offsetX": -0.45,
    "offsetY": -0.25
  }
}
